# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: db/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: db/test.sqlite3

production:
  <<: *default
  database: db/production.sqlite3
```

---

### Próximos Passos (Agora Vai Funcionar!)

Depois de criar e salvar todos esses arquivos nos locais corretos, seu projeto estará estruturado como o Rails espera.

Agora, volte ao seu terminal, na pasta do projeto, e tente novamente:

1.  **Reinstale o Bundle (boa prática para garantir que tudo está certo):**
    ```bash
    bundle install
    ```

2.  **Agora, execute o comando do banco de dados:**
    ```bash
    rails db:create
    
